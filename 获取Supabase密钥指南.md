# 🔑 获取 Supabase 密钥指南

## 📋 您的 Supabase 项目信息

**项目 URL**: `https://zrfnnerdaijcmhlemqld.supabase.co`  
**项目 ID**: `zrfnnerdaijcmhlemqld`

---

## 🎯 需要获取的密钥

我们需要获取您的 Supabase 项目的 **anon key**（匿名密钥），这是一个公开的密钥，用于前端应用访问 Supabase 服务。

---

## 📝 获取步骤（2分钟）

### 步骤1: 登录 Supabase Dashboard

1. 访问：https://supabase.com/dashboard
2. 使用您的账号登录

### 步骤2: 选择项目

1. 在项目列表中找到您的项目
2. 项目 URL 应该是：`https://zrfnnerdaijcmhlemqld.supabase.co`
3. 点击进入项目

### 步骤3: 进入项目设置

1. 点击左侧菜单的 **Settings**（设置）图标
2. 选择 **API** 选项

或直接访问：
```
https://supabase.com/dashboard/project/zrfnnerdaijcmhlemqld/settings/api
```

### 步骤4: 复制 API Keys

在 API 设置页面，您会看到两个密钥：

#### 1. Project URL（项目 URL）
```
https://zrfnnerdaijcmhlemqld.supabase.co
```
✅ 这个我们已经有了

#### 2. anon public（匿名公开密钥）
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS...
```
⚠️ **这个是我们需要的！**

**重要提示**：
- 这是一个很长的字符串（通常200+字符）
- 以 `eyJ` 开头
- 包含两个点号（`.`）分隔三个部分
- 这是 JWT（JSON Web Token）格式

### 步骤5: 提供密钥

请复制完整的 **anon public** 密钥，并提供给我。

---

## 🖼️ 截图参考

在 Supabase Dashboard 的 API 设置页面，您会看到类似这样的界面：

```
┌─────────────────────────────────────────────────────────────┐
│ Project API keys                                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ Project URL                                                 │
│ https://zrfnnerdaijcmhlemqld.supabase.co                   │
│ [Copy]                                                      │
│                                                             │
│ anon public                                                 │
│ This key is safe to use in a browser if you have enabled   │
│ Row Level Security for your tables and configured policies.│
│                                                             │
│ eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBh...  │
│ [Copy]                                                      │
│                                                             │
│ service_role secret                                         │
│ This key has the ability to bypass Row Level Security.     │
│ Never share it publicly.                                    │
│                                                             │
│ eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBh...  │
│ [Copy]                                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**请复制 "anon public" 下面的密钥**

---

## ⚠️ 安全提示

### 可以公开的密钥
✅ **anon public key** - 可以在前端代码中使用，可以公开

### 不能公开的密钥
❌ **service_role secret** - 绝对不能公开，只能在后端使用

---

## 🔄 更新配置

获取到密钥后，我会帮您更新以下文件：

### 1. .env 文件
```env
VITE_SUPABASE_URL=https://zrfnnerdaijcmhlemqld.supabase.co
VITE_SUPABASE_ANON_KEY=[您的 anon public key]
```

### 2. 数据库迁移

我会将所有数据库表和配置迁移到您的新 Supabase 项目。

---

## 📋 完整配置清单

更新配置后，我们需要：

- [x] 更新 Supabase URL
- [ ] 更新 Supabase anon key（等待您提供）
- [ ] 运行数据库迁移
- [ ] 启用 Email OTP 认证
- [ ] 配置管理员邮箱
- [ ] 测试登录功能

---

## 🎯 下一步

**请提供您的 Supabase anon public key**，格式如下：

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS...
```

获取到密钥后，我会立即：
1. 更新 .env 配置
2. 运行所有数据库迁移
3. 配置邮箱登录功能
4. 测试系统是否正常运行

---

## 🐛 常见问题

### Q: 找不到 API 设置页面？
A: 确保您已登录正确的 Supabase 账号，并选择了正确的项目。

### Q: 看不到 anon public key？
A: 检查您的账号权限，确保您是项目的所有者或管理员。

### Q: 密钥太长，复制不完整？
A: 点击密钥旁边的 [Copy] 按钮，会自动复制完整的密钥。

### Q: 不小心复制了 service_role secret？
A: 没关系，我们只需要 anon public key。请重新复制正确的密钥。

---

**等待您提供 anon public key...**
