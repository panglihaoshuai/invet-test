# 📧 邮箱登录配置指南

## 🎯 概述

系统已启用 Supabase Auth OTP（一次性密码）邮箱验证登录功能。本指南将帮助您配置和测试邮箱登录功能。

---

## ⚡ 快速开始（使用 Supabase 默认邮件服务）

### 当前状态
✅ **已启用** Supabase Email OTP 认证  
✅ **已配置** 登录页面使用 Supabase Auth  
✅ **已设置** 管理员邮箱自动分配

### 默认邮件服务限制
⚠️ **重要提示**：Supabase 默认邮件服务有以下限制：
- 每小时最多发送 **3 封邮件**
- 每天最多发送 **30 封邮件**
- 仅适用于开发和测试环境

### 立即测试（3分钟）

1. **启动应用**
   ```bash
   cd /workspace/app-7gjbw3zqrmdd
   npm run dev
   ```

2. **访问登录页面**
   - 打开浏览器访问：http://localhost:5173/login

3. **测试登录流程**
   ```
   步骤1: 输入邮箱地址（建议使用管理员邮箱）
          1062250152@qq.com
   
   步骤2: 点击"发送验证码"
   
   步骤3: 检查邮箱收件箱
          - 查找来自 noreply@mail.app.supabase.io 的邮件
          - 邮件主题：Confirm your signup
          - 复制邮件中的 6 位验证码
   
   步骤4: 输入验证码并点击"验证登录"
   
   步骤5: 登录成功！
          - 管理员邮箱 → 自动跳转到管理后台
          - 普通邮箱 → 跳转到首页
   ```

---

## 🔧 配置自定义 SMTP（推荐用于生产环境）

### 为什么需要自定义 SMTP？

使用自定义 SMTP 服务的优势：
- ✅ 无发送限制
- ✅ 自定义发件人名称和邮箱
- ✅ 更高的送达率
- ✅ 自定义邮件模板
- ✅ 更好的品牌形象

### 支持的 SMTP 服务

#### 1. Gmail SMTP（推荐用于个人项目）

**配置步骤**：

1. **启用 Gmail 两步验证**
   - 访问：https://myaccount.google.com/security
   - 启用"两步验证"

2. **生成应用专用密码**
   - 访问：https://myaccount.google.com/apppasswords
   - 选择"邮件"和"其他设备"
   - 生成密码并保存

3. **在 Supabase 中配置**
   - 登录 Supabase Dashboard
   - 进入项目设置 → Authentication → Email
   - 启用"Enable Custom SMTP"
   - 填写配置：
     ```
     SMTP Host: smtp.gmail.com
     SMTP Port: 587
     SMTP User: your-email@gmail.com
     SMTP Password: [应用专用密码]
     Sender Email: your-email@gmail.com
     Sender Name: 人格特质投资策略评估系统
     ```

#### 2. QQ 邮箱 SMTP（推荐用于国内用户）

**配置步骤**：

1. **开启 SMTP 服务**
   - 登录 QQ 邮箱
   - 设置 → 账户 → POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务
   - 开启"POP3/SMTP服务"或"IMAP/SMTP服务"
   - 生成授权码并保存

2. **在 Supabase 中配置**
   ```
   SMTP Host: smtp.qq.com
   SMTP Port: 587
   SMTP User: 1062250152@qq.com
   SMTP Password: [授权码]
   Sender Email: 1062250152@qq.com
   Sender Name: 人格特质投资策略评估系统
   ```

#### 3. SendGrid（推荐用于生产环境）

**优势**：
- 每天免费 100 封邮件
- 高送达率
- 详细的发送统计

**配置步骤**：

1. **注册 SendGrid**
   - 访问：https://sendgrid.com
   - 注册免费账号

2. **创建 API Key**
   - Settings → API Keys → Create API Key
   - 选择"Full Access"
   - 保存 API Key

3. **在 Supabase 中配置**
   ```
   SMTP Host: smtp.sendgrid.net
   SMTP Port: 587
   SMTP User: apikey
   SMTP Password: [SendGrid API Key]
   Sender Email: noreply@yourdomain.com
   Sender Name: 人格特质投资策略评估系统
   ```

#### 4. 阿里云邮件推送（推荐用于国内生产环境）

**优势**：
- 国内高送达率
- 每天免费 200 封邮件
- 支持自定义域名

**配置步骤**：

1. **开通服务**
   - 访问：https://www.aliyun.com/product/directmail
   - 开通邮件推送服务

2. **配置发信域名**
   - 添加并验证域名
   - 创建发信地址

3. **创建 SMTP 凭证**
   - 获取 SMTP 用户名和密码

4. **在 Supabase 中配置**
   ```
   SMTP Host: smtpdm.aliyun.com
   SMTP Port: 465
   SMTP User: [SMTP用户名]
   SMTP Password: [SMTP密码]
   Sender Email: noreply@yourdomain.com
   Sender Name: 人格特质投资策略评估系统
   ```

---

## 📝 Supabase SMTP 配置详细步骤

### 1. 登录 Supabase Dashboard

```
访问：https://supabase.com/dashboard
登录您的账号
选择项目：app-7gjbw3zqrmdd
```

### 2. 进入 Email 设置

```
左侧菜单 → Authentication → Email Auth
或直接访问：
https://supabase.com/dashboard/project/ahgnspudsmrvsqcinxcj/auth/email-templates
```

### 3. 启用自定义 SMTP

```
1. 找到"SMTP Settings"部分
2. 点击"Enable Custom SMTP"开关
3. 填写 SMTP 配置信息
```

### 4. 配置表单字段说明

| 字段 | 说明 | 示例 |
|------|------|------|
| **SMTP Host** | SMTP 服务器地址 | smtp.gmail.com |
| **SMTP Port** | SMTP 端口号 | 587 (TLS) 或 465 (SSL) |
| **SMTP User** | SMTP 用户名 | your-email@gmail.com |
| **SMTP Password** | SMTP 密码或授权码 | [应用专用密码] |
| **Sender Email** | 发件人邮箱 | noreply@yourdomain.com |
| **Sender Name** | 发件人名称 | 人格特质投资策略评估系统 |

### 5. 测试 SMTP 配置

```
1. 保存配置后，点击"Send Test Email"
2. 输入测试邮箱地址
3. 检查邮箱是否收到测试邮件
4. 如果收到，配置成功！
```

---

## 🎨 自定义邮件模板

### 1. 访问邮件模板设置

```
Supabase Dashboard → Authentication → Email Templates
```

### 2. 可自定义的模板

#### Confirm Signup（确认注册）

这是用户登录时收到的验证码邮件模板。

**默认模板**：
```html
<h2>Confirm your signup</h2>
<p>Follow this link to confirm your user:</p>
<p><a href="{{ .ConfirmationURL }}">Confirm your mail</a></p>
<p>Or enter this code: {{ .Token }}</p>
```

**推荐自定义模板**：
```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 30px;
      text-align: center;
      border-radius: 10px 10px 0 0;
    }
    .content {
      background: #f9f9f9;
      padding: 30px;
      border-radius: 0 0 10px 10px;
    }
    .code {
      background: white;
      border: 2px dashed #667eea;
      padding: 20px;
      text-align: center;
      font-size: 32px;
      font-weight: bold;
      letter-spacing: 8px;
      color: #667eea;
      margin: 20px 0;
      border-radius: 8px;
    }
    .footer {
      text-align: center;
      margin-top: 20px;
      color: #999;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>人格特质投资策略评估系统</h1>
  </div>
  <div class="content">
    <h2>您的登录验证码</h2>
    <p>您好！</p>
    <p>您正在登录人格特质投资策略评估系统，请使用以下验证码完成登录：</p>
    <div class="code">{{ .Token }}</div>
    <p><strong>验证码有效期：5分钟</strong></p>
    <p>如果这不是您的操作，请忽略此邮件。</p>
  </div>
  <div class="footer">
    <p>此邮件由系统自动发送，请勿回复</p>
    <p>© 2025 人格特质投资策略评估系统</p>
  </div>
</body>
</html>
```

### 3. 保存并测试模板

```
1. 在模板编辑器中粘贴自定义模板
2. 点击"Save"保存
3. 返回登录页面测试
4. 检查收到的邮件是否使用新模板
```

---

## 🐛 常见问题排查

### 问题1: 收不到验证码邮件

**可能原因**：
1. 邮箱地址输入错误
2. 邮件被标记为垃圾邮件
3. 达到 Supabase 默认邮件限制（每小时3封）
4. SMTP 配置错误

**解决方案**：

```bash
# 1. 检查垃圾邮件箱
# 查找来自 noreply@mail.app.supabase.io 的邮件

# 2. 检查 Supabase 日志
# Supabase Dashboard → Logs → Auth Logs
# 查看是否有发送失败的记录

# 3. 验证 SMTP 配置
# Supabase Dashboard → Authentication → Email Auth
# 点击"Send Test Email"测试

# 4. 检查邮件发送限制
# 如果使用默认服务，等待1小时后重试
# 或配置自定义 SMTP 服务
```

### 问题2: 验证码错误或已过期

**可能原因**：
1. 验证码输入错误
2. 验证码已过期（5分钟有效期）
3. 使用了旧的验证码

**解决方案**：

```
1. 仔细检查验证码是否正确
   - 验证码为6位数字
   - 注意区分相似字符（0和O，1和I）

2. 如果超过5分钟，重新获取验证码
   - 点击"重新发送"按钮
   - 等待60秒倒计时结束后可再次发送

3. 确保使用最新的验证码
   - 每次只有最新的验证码有效
   - 旧验证码会自动失效
```

### 问题3: SMTP 配置后仍无法发送

**检查清单**：

```
✓ SMTP Host 是否正确
✓ SMTP Port 是否正确（587 for TLS, 465 for SSL）
✓ SMTP User 是否正确
✓ SMTP Password 是否正确（注意是授权码，不是登录密码）
✓ Sender Email 是否与 SMTP User 一致
✓ 防火墙是否允许 SMTP 端口
✓ SMTP 服务是否已启用
```

**测试 SMTP 连接**：

```bash
# 使用 telnet 测试 SMTP 连接
telnet smtp.gmail.com 587

# 或使用 openssl 测试 SSL 连接
openssl s_client -connect smtp.gmail.com:465
```

### 问题4: Gmail SMTP 报错 "Username and Password not accepted"

**解决方案**：

```
1. 确认已启用两步验证
   访问：https://myaccount.google.com/security

2. 生成应用专用密码
   访问：https://myaccount.google.com/apppasswords
   - 不要使用 Gmail 登录密码
   - 必须使用应用专用密码

3. 检查 Gmail 安全设置
   - 允许不够安全的应用访问（如果需要）
   - 检查是否有登录尝试被阻止的通知

4. 使用正确的 SMTP 配置
   Host: smtp.gmail.com
   Port: 587
   User: your-email@gmail.com
   Password: [16位应用专用密码，无空格]
```

---

## 🔒 安全建议

### 1. 保护 SMTP 凭证

```bash
# ❌ 不要将 SMTP 密码提交到代码仓库
# ❌ 不要在前端代码中暴露 SMTP 凭证
# ✅ 仅在 Supabase Dashboard 中配置
# ✅ 使用环境变量管理敏感信息
```

### 2. 限制发送频率

```javascript
// 在前端添加发送限制
const [lastSendTime, setLastSendTime] = useState(0);

const handleSendCode = async () => {
  const now = Date.now();
  if (now - lastSendTime < 60000) {
    toast({
      title: '发送过于频繁',
      description: '请等待60秒后再试',
      variant: 'destructive'
    });
    return;
  }
  
  setLastSendTime(now);
  // ... 发送验证码逻辑
};
```

### 3. 验证码安全

```
✓ 验证码长度：6位数字
✓ 有效期：5分钟
✓ 一次性使用：验证后立即失效
✓ 限制尝试次数：防止暴力破解
```

---

## 📊 监控和日志

### 查看认证日志

```
Supabase Dashboard → Logs → Auth Logs

可以查看：
- 验证码发送记录
- 登录成功/失败记录
- 用户注册记录
- 错误信息
```

### 查看邮件发送统计

```
如果使用自定义 SMTP 服务：

Gmail: 
  - 查看已发送邮件文件夹

SendGrid:
  - Dashboard → Activity
  - 查看发送量、送达率、打开率

阿里云:
  - 控制台 → 邮件推送 → 数据统计
  - 查看发送量、成功率
```

---

## 🚀 生产环境部署建议

### 1. 配置自定义 SMTP

```
✓ 使用专业的邮件服务（SendGrid、阿里云等）
✓ 配置自定义域名
✓ 设置 SPF、DKIM、DMARC 记录
✓ 监控邮件送达率
```

### 2. 自定义邮件模板

```
✓ 使用品牌配色和 Logo
✓ 提供清晰的操作指引
✓ 添加联系方式和帮助链接
✓ 支持多语言
```

### 3. 优化用户体验

```
✓ 提供清晰的错误提示
✓ 显示验证码有效期
✓ 支持重新发送验证码
✓ 添加发送频率限制提示
```

### 4. 安全加固

```
✓ 启用 Rate Limiting
✓ 记录登录日志
✓ 监控异常登录行为
✓ 定期更新 SMTP 密码
```

---

## 📚 相关文档

- [管理员自动分配说明.md](./管理员自动分配说明.md) - 管理员配置指南
- [快速开始.md](./快速开始.md) - 系统快速测试指南
- [部署测试完整指南.md](./部署测试完整指南.md) - 完整部署说明

---

## ✅ 配置检查清单

### 基础配置
- [x] Supabase Email OTP 已启用
- [x] 登录页面已更新为使用 Supabase Auth
- [x] 管理员邮箱自动分配已配置
- [ ] 已测试默认邮件服务（待测试）

### 生产环境配置（可选）
- [ ] 已配置自定义 SMTP 服务
- [ ] 已自定义邮件模板
- [ ] 已配置自定义域名
- [ ] 已设置 SPF/DKIM/DMARC 记录

---

## 🎯 下一步

### 立即测试（推荐）

```bash
# 1. 启动应用
cd /workspace/app-7gjbw3zqrmdd
npm run dev

# 2. 访问登录页面
# 打开浏览器：http://localhost:5173/login

# 3. 使用管理员邮箱测试
# 邮箱：1062250152@qq.com
# 发送验证码 → 检查邮箱 → 输入验证码 → 登录成功
```

### 配置生产环境（可选）

```
1. 选择 SMTP 服务提供商
2. 获取 SMTP 凭证
3. 在 Supabase Dashboard 中配置
4. 测试邮件发送
5. 自定义邮件模板
6. 部署到生产环境
```

---

**配置完成时间**: 2025-01-10  
**当前状态**: ✅ 基础配置完成，可以使用 Supabase 默认邮件服务测试  
**建议**: 配置自定义 SMTP 服务以获得更好的体验
