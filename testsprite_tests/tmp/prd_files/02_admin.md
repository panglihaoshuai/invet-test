# 管理员与黑名单

## 目标
- 在系统设置页面输入邮箱设置为管理员（未注册也可，注册后自动生效）
- 可输入邮箱加入黑名单，限制登录

## 流程
1. 管理员后台页面：输入邮箱，点击“添加管理员”
2. 黑名单：输入邮箱，点击“拉黑”
3. 后端维护 `admin_emails` 与 `blocked_emails` 表，并提供 RPC 接口

## 权限与安全
- 仅管理员可访问系统设置页与相关 RPC
- 统一邮箱小写存储，避免大小写混乱

